# 課程設定（plan.json）格式說明

這份文件是 `plan.json` 的完整格式參考。這個檔案存在你自己的私有 Gist 裡，
不會進這個公開 repo。app 開啟時會透過 GitHub API 讀取它。

## 最上層欄位

```json
{
  "version": "1.0",
  "generated": "2026-09-03",
  "school": "學校名稱",
  "teacher": "教師姓名",
  "year": "2026–2027",

  "colors": { "班別id": "#hex顏色" },
  "groups": [ { "id": "g-xxx", "name": "完整名稱", "short": "分頁短名", "ids": ["班別id1", "班別id2"] } ],
  "classSize": { "班別id": 29 },
  "printClasses": [ { "id": "班別id", "name": "顯示名稱", "color": "#hex顏色" } ],
  "printRate": { "bw": 0.12, "color": 1.8 },

  "calendar": { "2026-09-01": ["開學禮"] },
  "weeks": { "2026-09-01": [1, "1"] },

  "classes": [ /* 見下面 */ ]
}
```

| 欄位 | 必填 | 說明 |
|---|---|---|
| `version` / `generated` | 建議填 | 版本號、產生日期，純標示用，方便你知道這份設定多新 |
| `school` / `teacher` / `year` | 選填但建議填 | 顯示在頁首小字，留空就不顯示那一段 |
| `colors` | 選填 | 每班在月曆上的顏色。留空會自動配色（CVD 友善色盤，按班別出現順序分配） |
| `groups` | 選填 | 「合併群組」——月曆分頁列會多出「總」以外的科目群組，例如同一班兩個科目、或同年級多班合併看。不需要就留空陣列 `[]` |
| `classSize` | 選填 | 「代印」分頁選好班別後，「份數」自動帶入的人數。不填就預設 1，使用者自己打 |
| `printClasses` | 選填 | 「代印」分頁的班別下拉選單。不填的話會直接用 `classes` 裡每一班（不合併科目）。如果同一個實際班分成多科目上課（像數學拆代數/幾何），這裡可以把它們合成一個選項 |
| `printRate` | 選填 | 影印單價（單面價，雙面自動乘 2）。不填就是 0，「代印」分頁金額會全部是 $0，但功能還能用（張數/份數/記錄都正常） |
| `calendar` | 必填（可空物件 `{}`） | 校曆事件：假期、考試週、活動等。日期字串對應一個事件名稱陣列，同一天可以放多個 |
| `weeks` | 必填（可空物件 `{}`） | 每個日期對應 `[學期, "週次"]`。週次是字串（因為有時要標特殊週，例如統測週）。月曆會顯示「第N週」，匯出 CSV 的學期/週次欄也是讀這裡 |
| `classes` | 必填 | 每一班的完整課節表，見下面 |

## `classes[]`：每一班

```json
{
  "id": "j1a-alg",
  "name": "初一甲 代數",
  "color": "#e34948",
  "textbook": "人教版 數學",
  "lessons": [ /* 見下面 */ ]
}
```

- `id`：這一班的唯一代號，之後 `colors`/`groups`/`classSize`/`printClasses` 都用這個 id 對應。建議用短英數字（如 `j1a-alg`、`j2c`），不要用會洩露班別真實資訊以外的東西（id 本身不會顯示給人看，`name` 才是）。
- `color`：也可以不寫在最上層 `colors`，直接寫在這裡，效果一樣（`colors` 優先）。
- `textbook`：純標示用，目前介面沒有特別顯示，留著方便你自己對照來源。

## `classes[].lessons[]`：每一節課

```json
{
  "id": "j1a-alg-001",
  "date": "2026-09-03",
  "period": "第 1 節",
  "time": "08:40–09:20",
  "plan": "1.1 正數和負數",
  "src": "課節表",
  "term": 1,
  "week": 1,
  "noPeriod": false
}
```

| 欄位 | 說明 |
|---|---|
| `id` | 這一節課的唯一 id，同一班內不要重複（建議 `班別id-流水號`） |
| `date` | `YYYY-MM-DD` |
| `period` | 第幾節，純文字顯示用，格式自訂（例如「第 1 節」），不影響任何邏輯計算 |
| `time` | 時間，純文字顯示用，可留空字串 |
| `plan` | 原定內容。可以用 `\n` 分好幾行——每一行會分別拿去跟下面的關鍵字比對，自動抓測驗/堂課/工作紙/功課等待辦事項 |
| `src` | 來源標記，純文字。介面上「課節表」不特別顯示標籤；「集備表推算」「待定」會顯示「推算・可改」／「待定」提示；「測驗安排」會顯示「測驗日」；「加開」是 app 自己加開課節時自動寫入的，不用你手動填 |
| `term` / `week` | 純資料標記，目前 CSV 匯出／統計不直接用這兩個欄位（用的是最上層的 `weeks` 對照表），但建議還是照實填，之後可能會用到 |
| `noPeriod` | 設 `true` 表示這筆記錄不是一個真正有節次的課堂時段（例如只是「交功課」的提醒），app 不會拿它去對應課表節次 |

`plan` 裡如果一行文字含有以下字眼，會被自動抓進「測驗・功課」分頁當待辦事項：
`統測`、`考試`、`測驗`、`堂課`、`實驗`、`工作紙`、`功課`／`作業`／（或其他你在 `TASK_RULES` 自訂的品牌作業名稱）。
數字建議都用阿拉伯數字，且跟前後中文字之間留空格（例如「工作紙 1」而不是「工作紙1」），純粹排版習慣，不影響功能。

## 最小可行範例

只有一班、沒有合併群組、沒有代印設定，也是合法的 `plan.json`：

```json
{
  "version": "1.0",
  "generated": "2026-09-01",
  "school": "示範中學",
  "teacher": "王老師",
  "year": "2026–2027",
  "calendar": {},
  "weeks": {},
  "classes": [
    {
      "id": "c1",
      "name": "一年甲班",
      "lessons": [
        { "id": "c1-001", "date": "2026-09-01", "period": "第 1 節", "time": "", "plan": "第一課", "src": "課節表", "term": 1, "week": 1, "noPeriod": false }
      ]
    }
  ]
}
```
